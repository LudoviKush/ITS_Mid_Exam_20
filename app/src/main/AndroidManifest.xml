<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.provabiblioteca2020">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.List02"></activity>
        <activity android:name=".activity.Mod01" />
        <activity android:name=".activity.New01" />
        <activity android:name=".activity.List01" />
        <activity android:name=".activity.Home01">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name=".data.LibriMovimentiProvider"
            android:authorities="com.example.provabiblioteca2020.LibriMovimentiProvider"
            android:exported="false" />
    </application>

</manifest>